HTML = Hyper Text Markup Language
Tags:

<meta charset="UTF-8">   Para resolver problemas de formatação

<h1></h1> Exibir o primeiro título de uma página. 

<br> Pular linha

<a href="insira_link">insira_conteudo</a> Adicionar link - nos permite sair de uma página e mudar para outra

<script></script> Trocar a leitura de linguagem da maquina - Dessa forma o navegador saberá que deve processar essa parte do código como linguagem JavaScript e não como HTML

alert ("texto_pop-up"); Criar pop-up

Uma convenção muito utilizada é criar arquivos com letras minúsculas e se houver mais de uma palavra usamos um "_" como separador. Além disso, não usamos acentos no nome dos arquivos.

Ctrl + O para abrir um arquivo no navegador

String=Texto

Concatenação é quando temos uma operação de soma envolvendo texto, o JavaScript não entende se tratar de um número.

parseInt(texto) = Converte o texto em número

console.log(numeroPensado)

switch() - if else mais robusto

for() possui a mesma funcionalidade do while(), com a diferença de que ela é divida em três partes. A primeira é uma variável para inicializá-lo, a que será incrementada, o segundo contém a condição de repetição, a última parte do for() é o incremento da variável multiplicador, declarada no espaço1

parseFloat() converter texto em “números reais”, ou seja, respeita as decimais

document.querySelector que podemos ir lá no mundo HTML e trazer o elemento para o mundo JavaScript para que possamos manipulá-lo

array - é de bom sempre usar no plural

.lenght no array declara quantos conteudos ele tem, sem necessidade de ficar mudando sempre que mudar a quantidade